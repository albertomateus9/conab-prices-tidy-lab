[project]
name = "conab-prices-tidy-lab"
version = "0.1.0"
description = "Tidy data lab for transforming wide price tables into long format and calculating variation."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Alberto Mateus P. da Gama" }]

[tool.pytest.ini_options]
pythonpath = ["."]
